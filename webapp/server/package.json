{
  "name": "acp-orchestrator-server",
  "version": "1.0.0",
  "description": "ACP Orchestrator Server - manages Copilot CLI agents across repos",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file-if-exists=../../.env index.js",
    "dev": "node --watch --env-file-if-exists=../../.env index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.20.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pino": "^10.3.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.5",
    "vitest": "^4.0.18"
  }
}
