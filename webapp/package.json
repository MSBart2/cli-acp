{
  "name": "acp-orchestrator",
  "version": "1.0.0",
  "private": true,
  "description": "ACP Agent Orchestrator - Web UI for managing Copilot CLI agents across repositories",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently --kill-others --names server,client --prefix-colors blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm run install:all && cd client && npm run build",
    "start": "cd server && npm start",
    "prod": "npm run build && npm start",
    "teardown": "node scripts/teardown.js",
    "test": "cd server && npm test && cd ../client && npm test",
    "test:server": "cd server && npm test",
    "test:client": "cd client && npm test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
